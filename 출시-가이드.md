# 「오늘의 말씀」 출시 가이드

이 폴더는 스토어 출시가 가능한 **PWA(설치형 웹앱)** 구성입니다.
코드는 준비가 끝났고, 아래 단계는 계정·심사 문제라 하울님이 직접 진행해야 합니다.

## 폴더 구성

| 파일 | 역할 |
|---|---|
| `index.html` | 앱 본체 (온보딩 + 오늘의 말씀 + 알림/백업/설치) |
| `manifest.webmanifest` | 앱 이름·아이콘·색상 정의 (설치형 앱의 신분증) |
| `sw.js` | 서비스워커 — 오프라인 동작 + 알림 클릭 처리 |
| `icon-192.png` / `icon-512.png` | 앱 아이콘 |
| `privacy.html` | 개인정보처리방침 (스토어 필수) — **문의 이메일 채워 넣을 것** |

## 1단계 — 무료 호스팅 (필수, 약 30분)

스토어 등록(TWA)은 HTTPS로 호스팅된 PWA가 필요합니다. GitHub Pages가 무료로 가장 간단합니다.

1. github.com 가입 → 새 저장소 생성 (예: `daily-verse`, Public)
2. 이 폴더의 파일 5개 + 아이콘 2개를 업로드
3. 저장소 Settings → Pages → Branch를 `main`으로 설정
4. 몇 분 뒤 `https://<아이디>.github.io/daily-verse/` 접속 확인
5. 휴대폰 크롬으로 접속 → "홈 화면에 추가"가 뜨면 PWA 인증 완료

## 2단계 — 구글 플레이스토어 (TWA)

1. **PWABuilder 사용**: https://www.pwabuilder.com 에 1단계 URL 입력
   → Android 패키지(.aab) 자동 생성 (서명 키도 함께 발급됨 — **키 파일은 절대 분실 금지**)
2. **Google Play Console** 개발자 계정 등록 — $25 일회성
3. 앱 만들기 → .aab 업로드 → 스토어 등록정보 작성
4. PWABuilder가 안내하는 `assetlinks.json` 파일을 1단계 저장소의 `.well-known/` 폴더에 업로드 (이게 없으면 앱 상단에 브라우저 주소창이 보임)
5. 심사 제출 (보통 2~7일)

### 스토어 등록에 필요한 자료 (미리 준비)
- 스크린샷 2장 이상 (휴대폰에서 캡처, 1080×1920 권장)
- 512×512 아이콘 → `icon-512.png` 그대로 사용 가능
- 그래픽 이미지 1024×500 1장 (앱 배경 + 제목 텍스트로 제작)
- 짧은 설명(80자), 전체 설명(4000자)
- 개인정보처리방침 URL → `https://<아이디>.github.io/daily-verse/privacy.html`

## 3단계 — 애플 앱스토어 (선택)

- Apple Developer Program 연 $99 필요
- PWABuilder의 iOS 패키지 또는 Capacitor로 래핑 → Xcode(맥 필요)로 제출
- 애플은 "웹사이트를 감싼 앱"에 심사가 깐깐해서, 알림·위젯 같은 네이티브 기능을 더한 뒤 도전하는 걸 권합니다. **먼저 플레이스토어로 시작하세요.**

## 꼭 알아야 할 것들

### 성경 번역 저작권
- 앱에 실린 구절은 **개역한글판**(저작권 만료)이라 상업 배포에 문제가 없습니다.
- **개역개정으로 바꾸려면 대한성서공회 사용 허락이 필요합니다** (유료 라이선스). 무단 사용 시 앱이 내려갈 수 있어요.
- 누가복음 1:38 한 구절만 옛 표현("계집종")이 어색해 "여종"으로 다듬어져 있습니다.

### 알림의 한계 (솔직한 설명)
- 현재 구현: 앱(또는 설치된 PWA)이 **열려 있을 때** 정해진 시간에 알림 + 일부 안드로이드 크롬에서는 닫혀 있어도 다음 1회 예약 알림.
- 원본 #Bible 앱 같은 **완전한 매일 푸시 알림**은 서버 푸시(FCM) 또는 Capacitor 네이티브 로컬 알림이 필요합니다. 사용자가 늘면 그때 업그레이드하면 됩니다 (Capacitor 전환 시 이 코드 그대로 재사용 가능).

### 브랜딩 주의
- "#Bible" 이름·아이콘·스크린샷 문구를 따라 하면 안 됩니다. "오늘의 말씀"이라는 독자 이름과 자체 제작 아이콘을 쓰고 있으므로 현재 상태는 안전합니다.
- 스토어 등록 시 앱 이름이 겹치면 "오늘의 말씀 — 매일 성경 한 구절" 같은 부제를 붙이세요.

## 출시 전 체크리스트

- [ ] `privacy.html`의 문의 이메일 채우기
- [ ] GitHub Pages 배포 후 휴대폰에서 설치 테스트
- [ ] 온보딩 → 메인 → 알림 켜기 → 저장/공유 전부 눌러보기
- [ ] 스크린샷 촬영
- [ ] Play Console 계정 + $25 결제
- [ ] PWABuilder로 .aab 생성 + assetlinks.json 업로드
- [ ] 심사 제출
